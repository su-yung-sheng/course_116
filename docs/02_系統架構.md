# 02 系統架構

## 資料夾

```
course_116/
├── index.html                總入口：依 semester.js 的日期倒數 3 秒導向（?term=11601、?stay）
├── shared/                ★  兩學期共用，全站只有一份
│   ├── theme.css               全站樣式（方格紙＋紙膠帶，延續 115；不依賴 Tailwind）
│   ├── semester.js             學期界線（116-1：2027-08-01～2028-01-31）
│   ├── store.js           ★   身分與進度（目前是本機版，見下）
│   ├── ui.js                   星星、愛心、提示、對話框、登入閘門、頁首
│   ├── hub.js                  闖關地圖引擎（內容來自 config.js；單元可用 parts 包好幾個部分）
│   ├── unit.js                 課程小卡（UNIT.cards）：單元首頁與各部分頁面上方的導覽
│   ├── pyrunner.js        ★   Python 執行／評分／錯誤翻譯（主執行緒）
│   ├── pyworker.js        ★   Pyodide Web Worker（跑學生的程式）
│   ├── pyapp.js .css           Python 闖關頁（11601/python.html 只是殼）
│   ├── cardgame.js .css   ★   互動體驗遊戲引擎：sort／order／build／type／gen🎲（單元三、四、五、六）
│   ├── sheet.js .css      ★   迷你試算表：公式計算、COUNTIF、資料清理關卡
│   ├── cipher-gen.js           🎲 密碼特務的隨機出題器（cardgame 的 gen 回合）
│   ├── labkit.js .css          5016B 實驗室引擎：分頁、先預測再驗證、成果卡
│   └── seal.js            ★   答案封存：SHA-256 雜湊、AES-GCM 封存（見 docs/05_安全性.md）
├── 11601/                    上學期
│   ├── config.js               ★ 這學期唯一的設定檔（MODULES、RANKS、PYODIDE_URL、OPEN_ALL）
│   ├── hub.html                闖關地圖
│   ├── digital/                單元一（unforte_114 移植＋progress-hook.js）
│   ├── python.html             單元二
│   ├── platform.html           單元三
│   ├── 5016b.html              5016B 專題
│   └── content/
│       ├── python.js           ★ 單元二關卡與測資
│       └── platform.js         ★ 單元三關卡內容
├── 11602/                    下學期
│   ├── config.js hub.html
│   ├── media.html              單元四（概念闖關＋專題工作站）
│   ├── network.html            單元五
│   ├── unit6.html              單元六首頁（三張課程小卡）
│   ├── data.html sheet.html cipher.html   單元六的三部分（概念、試算表、密碼特務🎲）
│   ├── 5016b.html              5016B 守護站 2.0
│   └── content/                media.js network.js data.js sheet.js cipher.js
├── private/                  🔒 明碼答案（不進 Git，老師自己備份）── 見 private/README.md
├── tools/build.mjs           private/ → 封存後的 content/*.js、content/lab.js、digital/1～4.html 的 data-k
├── tests/                    Playwright 自動測試＋leak_scan.mjs（見 README）
└── docs/
```

**原則同 course_115**：兩學期資料夾結構一樣，差別只在 `config.js` 與 `content/`；`shared/` 只有一份，用 `../shared/xxx.js` 引用，沒有副本就不會漂移。
**內容與引擎分離**：改教什麼 → `private/…/content/*.js` 再 `node tools/build.mjs`；改版型或規則 → `shared/`。公開的 `content/*.js` 是建置產物，裡面沒有明碼答案。
各單元頁面只剩「載入內容＋呼叫引擎」幾行，例如：

```js
CARDGAME.mount({ app: '#app', levels: NET_LEVELS, mod: 'network' });
PYAPP.mount({ root: '#pyapp', levels: PY_LEVELS, mod: 'python', title, kicker, subtitle, finale });
SHEET.mount({ app: '#app', levels: SHEET_LEVELS, mod: 'sheet' });
LABKIT.mount({ sections: SECTIONS, mod: 'arduino' });
```

## 進度：`window.STORE`（本機版）

所有頁面只透過這組 API 讀寫進度：

```js
STORE.me()                                   // { cls:'901', seat:'05', name } 或 null
STORE.login(cls, seat, name) / logout()
STORE.level('python', 'P3')                  // { stars, score, tries, at, code }
STORE.saveLevel('python', 'P3', { stars, score, code })   // 星星只會變多（取最佳）
STORE.moduleStars('python') / moduleDone('python')
STORE.draft(key, value)                      // 編輯器草稿
STORE.exportJSON() / importJSON(text)        // 換電腦時帶著走
```

資料形狀刻意做成和 115 的 Firestore 文件一樣：

```
{學期}-progress/{學號} = { modules: { python: { levels: { P3: { stars: 3, score: 100, … } } } } }
```

### ⚠️ 本機版的限制

- 進度存在「這台電腦的這個瀏覽器」—— **電腦教室有還原卡的話，關機就清掉**
- 學生按 F12 可以改自己的星星 —— 本機版的星星是**學習回饋**，不是成績
- 沒有教師端

### 正式上課前：接回 115 的後端（第二階段）

只重寫 `shared/store.js`，**API 名稱不變，各單元頁面一行都不用改**：

1. 從 course_115 搬 `auth.js`、`guard.js`、`sso.js`、`firestore.rules`（學校 Google 帳號登入、`isOwner(sid)`）
2. `STORE.me()` 改讀 `AUTH`；`saveLevel()` 改成 Firestore `merge` 寫入 `11601-progress/{學號}`
3. `firestore.rules` 補上各模組的欄位（116-1：`digital` `python` `platform` `arduino`；116-2：`media` `network` `data` `sheet` `python` `arduino`）
4. `ui.js` 的 `requireLogin()` 換成 Google 登入按鈕
5. 搬 `teacher.html`：Python 那一格可以直接看 `record.code`（最後一次送出的程式）
6. 名冊 `roster` 沿用（同一批學生）；學期鎖日期照 `semester.js`

## 依序開放

只有 Python（116-1 單元二、116-2 密碼特務）是依序開放（上一關 ≥ 2⭐ 才開下一關），判斷只看 `STORE` 的星數。
備課時要看全部關卡：`config.js` 設 `OPEN_ALL_UNITS: true` **並且一定要設 `OPEN_ALL_UNTIL` 到期日**，過期自動失效；開著的時候闖關地圖會掛橘色橫幅。

## 外部相依

| 什麼 | 從哪來 | 擋掉時 |
|---|---|---|
| Pyodide 0.26.4 | `cdn.jsdelivr.net`（`config.js` 的 `PYODIDE_URL`） | 下載整包放 `shared/pyodide/`，`PYODIDE_URL` 改 `'../shared/pyodide/'` |
| Noto Sans TC | Google Fonts | 自動退回系統字型，版面不受影響 |
| Tailwind CDN | 只有單元一（unforte_114 原樣） | 單元一版面會跑掉，其他頁不受影響 |

## 本機預覽

不要直接雙擊 HTML（`file://` 下 Web Worker 會被瀏覽器擋，Python 引擎起不來）。在 repo 根目錄：

```
python -m http.server 8116
```

開 `http://localhost:8116/`。
